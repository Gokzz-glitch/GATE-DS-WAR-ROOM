import json
import os

# Mock dataset of GATE DA PYQs and explanations
DATA = [
    {
        "messages": [
            {"role": "user", "content": "What is the time complexity of building a heap from an array of n elements?"},
            {"role": "model", "content": "Building a heap (min-heap or max-heap) from an unsorted array of n elements takes O(n) time. While inserting elements one by one takes O(n log n), the bottom-up heapify approach (sift-down) bounds the sum of heights to O(n)."}
        ]
    },
    {
        "messages": [
            {"role": "user", "content": "Explain the Bias-Variance tradeoff in Machine Learning."},
            {"role": "model", "content": "The bias-variance tradeoff is a fundamental property of predictive models. Bias represents the error from erroneous assumptions in the learning algorithm (underfitting), while Variance represents the error from sensitivity to small fluctuations in the training set (overfitting). As model complexity increases, bias decreases but variance increases. The goal is to find the sweet spot that minimizes total error."}
        ]
    },
    {
        "messages": [
            {"role": "user", "content": "What is the rank of a matrix?"},
            {"role": "model", "content": "The rank of a matrix is the maximum number of linearly independent row vectors or column vectors in the matrix. It represents the dimension of the vector space generated by its columns (or rows)."}
        ]
    },
    {
        "messages": [
            {"role": "user", "content": "How do you handle missing data in a dataset?"},
            {"role": "model", "content": "Missing data can be handled in several ways depending on the context: 1. Deletion (listwise or pairwise) if the missing rate is very small. 2. Mean/Median/Mode imputation. 3. Predictive imputation (e.g., using KNN or regression). 4. Forward/Backward fill for time-series data. 5. Using algorithms that inherently handle missing values like XGBoost."}
        ]
    },
    {
        "messages": [
            {"role": "user", "content": "What is the difference between primary key and unique key in DBMS?"},
            {"role": "model", "content": "A Primary Key uniquely identifies a record and cannot accept NULL values. A table can have only one primary key. A Unique Key also uniquely identifies a record, but it can accept a single NULL value (in most SQL implementations). A table can have multiple unique keys."}
        ]
    }
]

def generate_jsonl():
    os.makedirs("data", exist_ok=True)
    file_path = "data/training_data.jsonl"
    
    with open(file_path, "w") as f:
        for entry in DATA:
            f.write(json.dumps(entry) + "\n")
            
    print(f"Generated {len(DATA)} training examples at {file_path}")
    print("Ready for tuning!")

if __name__ == "__main__":
    generate_jsonl()
